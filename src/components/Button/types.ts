import { BaseButtonProps } from 'components/BaseButton/types'

export interface ButtonProps extends BaseButtonProps {
  color?: 'blue' | 'gray-light' | 'gray-dark' // TODO: melhorar esquema de variantes
}
